{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Shared capability for every Unsit window",
  "windows": ["settings", "popup", "notice", "flyout", "overlay-*"],
  "permissions": ["core:default", "notification:default"]
}
